package models.enums;

/**
 * Categorizes {@link models.enums.Topping} options based on their type.
 * Used to organize toppings into logical groups such as meat, cheese, regular, and sauce.
 */
public enum ToppingType {
    MEAT,
    CHEESE,
    REGULAR,
    SAUCE
}
